{
  "name": "jungle-system",
  "private": true,
  "version": "0.1.0",
  "description": "多 Agent 协作平台 —— 接入 Hermes Agent 与 Claude Code，提供前端编排与实时协作",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @jungle/server dev",
    "dev:web": "pnpm --filter @jungle/web dev",
    "dev:desktop": "pnpm --filter @jungle/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "smoke": "pnpm --filter @jungle/cli smoke",
    "cli": "pnpm --filter @jungle/cli run"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "electron-winstaller",
      "esbuild"
    ]
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
